# Example env file for product-query-gen (no secrets)
# Copy to `.env` and fill in real values locally. Do NOT commit `.env`.

# OpenAI
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Behavior & quality
LLM_SELF_CHECK=1
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=600

# Logging & concurrency
LOG_LEVEL=INFO
CONCURRENCY_LIMIT=5
